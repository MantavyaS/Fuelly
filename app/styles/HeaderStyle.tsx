import { StyleSheet } from "react-native";

const HeaderStyle = StyleSheet.create({
  container: {
    gap: 5,
    color: "#FF6600",
    fontSize: 50,
    fontFamily: "poppins",
    fontWeight: "bold",
    textAlign: "center",
  },
});

export default HeaderStyle;